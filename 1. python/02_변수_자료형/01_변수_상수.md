
## 변수 (Variables) 
	- 메모리에 데이터를 저장하기 위해 사용되는 지정된 위치 
	
	- 변수에 데이터/값을 할당하기 위해서는 = 기호 사용 
	
	- 파이썬은 'type-inferred(타입 추론)' 언어로
	   변수의 유형(자료형)을 명시적으로 정의할 필요가 없다.
	
	- 변수 선언 및 값 변경 
```
website ="kopo.ac.kr"
print(website)

-> kopo.ac.kr
```

	- 변수의 내용물은 언제든 변경 가능
```
website ="kopo.ac.kr"
print(website)
website = "naver.com"
print(website)
website = 1
print(website)

-> kopo.ac.kr 
   naver.com 
   1
```

	- 여러변수에 값 할당하기
```
# 1. 각각 다른 값을 한번에 할당할 때
a, b, c = 10, 5.2, "kopo"
print(a) 
print(b) 
print(c)

-> 10 
   5.2 
   kopo
   
# 2. 여러 변수에 같은 값을 동시에 할당할 때
a = b = c = "kopo" 
print(a) 
print(b) 
print(c)

-> kopo 
   kopo 
   kopo
```

## 상수 (Constants)
	- 모듈에서 선언되고 할당된다.
		
![[Contants_module_1.png|111]] ![[Contants_module_2.png|161]]

	- 값을 고정하는 '상수' 를 사용하지 않기 때문에 재할당을 방지하는 기능이 없다.

	- 상수의 이름은 대문자와 _ 기호의 조합으로 작성
